# This is a toolbox repo (no app to run). devDependencies exist only so the IDE can
# resolve types for the MEDA UI templates. Skip pnpm's pre-script dependency check so
# `pnpm typecheck` doesn't fail over native build scripts we intentionally don't build.
verify-deps-before-run=false
